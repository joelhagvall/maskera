diff --git a/dist/transformers.js b/dist/transformers.js
index 3274d9cfc2453aeb6b1ee64e61c85f6984aaad88..707d41e21ed533de2ed9ae7c2ccc9d8554a9198f 100644
--- a/dist/transformers.js
+++ b/dist/transformers.js
@@ -6389,9 +6389,9 @@ function get_file_metadata(path_or_repo_id, filename, options = {}) {
   const key = JSON.stringify([
     path_or_repo_id,
     filename,
-    options?.revision,
-    options?.cache_dir,
-    options?.local_files_only
+    options?.revision ?? "main",
+    options?.cache_dir ?? null,
+    options?.local_files_only ?? false
   ]);
   return memoizePromise(key, () => _get_file_metadata(path_or_repo_id, filename, options));
 }
@@ -6422,6 +6422,10 @@ async function _get_file_metadata(path_or_repo_id, filename, options) {
         if (typeof response !== "string" && response.status !== 404) {
           const size = response.headers.get("content-length");
           const contentType = response.headers.get("content-type");
+          try {
+            await response.body?.cancel();
+          } catch (cancelError) {
+          }
           return {
             exists: true,
             size: size ? parseInt(size, 10) : void 0,
diff --git a/dist/transformers.node.cjs b/dist/transformers.node.cjs
index 6cb09aecb7364f1b909fb6ceed8daff52bba345b..7bd9f4977bb25207e4b26ab63bbbd5f854fb1523 100644
--- a/dist/transformers.node.cjs
+++ b/dist/transformers.node.cjs
@@ -7347,9 +7347,9 @@ function get_file_metadata(path_or_repo_id, filename, options = {}) {
   const key = JSON.stringify([
     path_or_repo_id,
     filename,
-    options?.revision,
-    options?.cache_dir,
-    options?.local_files_only
+    options?.revision ?? "main",
+    options?.cache_dir ?? null,
+    options?.local_files_only ?? false
   ]);
   return memoizePromise(key, () => _get_file_metadata(path_or_repo_id, filename, options));
 }
@@ -7380,6 +7380,10 @@ async function _get_file_metadata(path_or_repo_id, filename, options) {
         if (typeof response !== "string" && response.status !== 404) {
           const size = response.headers.get("content-length");
           const contentType = response.headers.get("content-type");
+          try {
+            await response.body?.cancel();
+          } catch (cancelError) {
+          }
           return {
             exists: true,
             size: size ? parseInt(size, 10) : void 0,
diff --git a/dist/transformers.node.mjs b/dist/transformers.node.mjs
index bacb354fe1b898d4c535a39f5ef1ba5c6a463d75..9015ec63eaf26ddc452a84e0d1cbd401e48b546a 100644
--- a/dist/transformers.node.mjs
+++ b/dist/transformers.node.mjs
@@ -6388,9 +6388,9 @@ function get_file_metadata(path_or_repo_id, filename, options = {}) {
   const key = JSON.stringify([
     path_or_repo_id,
     filename,
-    options?.revision,
-    options?.cache_dir,
-    options?.local_files_only
+    options?.revision ?? "main",
+    options?.cache_dir ?? null,
+    options?.local_files_only ?? false
   ]);
   return memoizePromise(key, () => _get_file_metadata(path_or_repo_id, filename, options));
 }
@@ -6421,6 +6421,10 @@ async function _get_file_metadata(path_or_repo_id, filename, options) {
         if (typeof response !== "string" && response.status !== 404) {
           const size = response.headers.get("content-length");
           const contentType = response.headers.get("content-type");
+          try {
+            await response.body?.cancel();
+          } catch (cancelError) {
+          }
           return {
             exists: true,
             size: size ? parseInt(size, 10) : void 0,
diff --git a/dist/transformers.web.js b/dist/transformers.web.js
index bb7b32717a42475a5f66411a33d5e5964dfda3e2..962b6ea086ac6c71fb73464e412f80852fc95023 100644
--- a/dist/transformers.web.js
+++ b/dist/transformers.web.js
@@ -6383,9 +6383,9 @@ function get_file_metadata(path_or_repo_id, filename, options = {}) {
   const key = JSON.stringify([
     path_or_repo_id,
     filename,
-    options?.revision,
-    options?.cache_dir,
-    options?.local_files_only
+    options?.revision ?? "main",
+    options?.cache_dir ?? null,
+    options?.local_files_only ?? false
   ]);
   return memoizePromise(key, () => _get_file_metadata(path_or_repo_id, filename, options));
 }
@@ -6416,6 +6416,10 @@ async function _get_file_metadata(path_or_repo_id, filename, options) {
         if (typeof response !== "string" && response.status !== 404) {
           const size = response.headers.get("content-length");
           const contentType = response.headers.get("content-type");
+          try {
+            await response.body?.cancel();
+          } catch (cancelError) {
+          }
           return {
             exists: true,
             size: size ? parseInt(size, 10) : void 0,
diff --git a/src/utils/model_registry/get_file_metadata.js b/src/utils/model_registry/get_file_metadata.js
index f07bb4ba8d0bcab3e9b9377bcd4b82cb80f023c1..daee3b402ee0f503f9dd9ded59b90b3f3ad04fa2 100644
--- a/src/utils/model_registry/get_file_metadata.js
+++ b/src/utils/model_registry/get_file_metadata.js
@@ -54,9 +54,9 @@ export function get_file_metadata(path_or_repo_id, filename, options = {}) {
     const key = JSON.stringify([
         path_or_repo_id,
         filename,
-        options?.revision,
-        options?.cache_dir,
-        options?.local_files_only,
+        options?.revision ?? 'main',
+        options?.cache_dir ?? null,
+        options?.local_files_only ?? false,
     ]);
     return memoizePromise(key, () => _get_file_metadata(path_or_repo_id, filename, options));
 }
@@ -94,6 +94,14 @@ async function _get_file_metadata(path_or_repo_id, filename, options) {
                     const size = response.headers.get('content-length');
                     const contentType = response.headers.get('content-type');
 
+                    // This request is only for the headers: cancel the body so a
+                    // dangling full download does not race the real one.
+                    try {
+                        await response.body?.cancel();
+                    } catch (cancelError) {
+                        // Ignore: body may not be cancellable (e.g. FileResponse).
+                    }
+
                     return {
                         exists: true,
                         size: size ? parseInt(size, 10) : undefined,
